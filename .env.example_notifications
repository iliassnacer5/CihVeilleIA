# =================================================================
# CIH-VEILLE-IA - CONFIGURATION DES NOTIFICATIONS (Outlook SMTP)
# =================================================================
# Pas besoin d'App Registration Azure !
# Utilisez simplement vos identifiants Outlook / Office 365.

# --- Outlook SMTP ---
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=Iliass.Nacer@emsi-edu.ma
SMTP_PASSWORD=votre_mot_de_passe_outlook
SMTP_USE_TLS=True

# Si vous avez la 2FA activée sur votre compte Microsoft :
# 1. Allez sur https://account.live.com/proofs/AppPassword
# 2. Générez un "App Password" dédié
# 3. Utilisez ce mot de passe ci-dessus au lieu de votre vrai mot de passe

# --- Paramètres de Logique Veille ---
# Envoi automatique si score RAG > 80%
NOTIFICATION_RAG_THRESHOLD=0.80
# Anti-spam : Pas de notification pour le même contenu pendant 1 heure
NOTIFICATION_DEDUPLICATION_WINDOW=3600
# Nombre de tentatives en cas d'erreur réseau
NOTIFICATION_RETRY_ATTEMPTS=3

# --- Environnement ---
ENV=production
DEBUG=False
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=VeillePlus
